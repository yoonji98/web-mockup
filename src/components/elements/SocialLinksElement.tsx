import { MenuElement } from "@/components/elements/MenuElement";
import type { ElementComponentProps } from "@/components/elements/types";

export function SocialLinksElement(props: ElementComponentProps) {
  return <MenuElement {...props} />;
}
